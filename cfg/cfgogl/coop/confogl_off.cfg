// Disable ReadyUp
l4d_ready_enabled "0"   // This cvar doesnt do anything, but if it is 0 the logger wont log this game.

// Reset Default Common Limit, Static Spawns, and String Count
reset_static_maps
sm_resetstringcount

// Unlock Plugins and reload defaults
confogl_resetclientcvars
confogl_resetcvars
pred_unload_plugins
sm plugins load l4d2_sv_tags.smx  //互联网显示服务器（比赛结束后重新加载，保证空闲时也在列表里）  //互联网显示服务器（比赛结束后重新加载，保证空闲时也在列表里）（比赛结束后重新加载，保证空闲时也在列表里）
